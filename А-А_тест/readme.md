**Анализ А/А-теста**

1. Определение количества уникальных пользователей, которые участвовали в эксперименте.

2. Определение контрольных групп (А1, А2).

3. Определение в датасете  пользователей, которые попали в обе группы, их количество (таких нет, сплитование корректно).

4. Проверка метрик:

   Проверка разницы в количестве пользователей в обоих группах  (0.7% < 1%) - условие соблюдается.

   Проверка ARPU по группам - одинаковое. 

   Проверка среднего количества покупок по группам - одинаковое.
   
   Проверка среднего чека по группам - одинаковые.

5. Проверка гипотез об отличии групп в А/А-тесте.

   p_value=0.407 > 0.05 - гипотезу о равенстве распределения выручки в группах A1 и A2 не отвергаем с уровнем значимости a=0.05.

   p_value=0.23 > 0.05 - гипотезу о равенстве распределения количества покупок в группах A1 и A2 не отвергаем с уровнем значимости a=0.05.

   p_value=0.145 > 0.05 - гипотезу о равенстве распределения суммарного дохода от пользователя в группах A1 и A2 не отвергаем с уровнем значимости a=0.05.

Вывод по 3-м тестам: группы статистически значимо не отличаются - система сплитования сработала корректно, метрика пригодна и для А/В тестирования с уровнем значимости a=0.05.
